<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Responsive Webpage</title>
    <style>
        /* General Reset */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: Arial, sans-serif;
            display: flex;
            flex-direction: column;
            min-height: 100vh;
            background-color: #f4f4f9;
        }

        /* Navigation Styles (Flexbox) */
        .navbar {
            display: flex;
            justify-content: space-between;
            align-items: center;
            background-color: #333333;
            padding: 15px 30px;
            color: white;
        }

        .navbar h1 {
            font-size: 24px;
        }

        .navbar nav a {
            color: white;
            text-decoration: none;
            margin: 0 15px;
        }

        /* Main Layout (CSS Grid) */
        .container {
            display: grid;
            grid-template-columns: 1fr 3fr;
            gap: 20px;
            padding: 20px;
            flex: 1;
        }

        .sidebar {
            background-color: #2a9d8f;
            padding: 20px;
            border-radius: 8px;
            color: white;
        }

        .content {
            background-color: #ffffff;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.1);
        }

        /* Footer Styles */
        footer {
            background-color: #333333;
            color: white;
            text-align: center;
            padding: 10px;
        }

        /* Media Queries for Responsiveness */
        @media (max-width: 768px) {
            .container {
                grid-template-columns: 1fr; /* Single column for smaller screens */
            }
            .navbar nav {
                display: none; /* Hide navigation links on smaller screens */
            }
        }

        @media (min-width: 769px) and (max-width: 1024px) {
            .navbar nav a {
                font-size: 14px;
            }
        }
    </style>
</head>
<body>

    <!-- Navigation Bar (Flexbox) -->
    <header class="navbar">
        <h1>My Webpage</h1>
        <nav>
            <a href="#home">Home</a>
            <a href="#about">About</a>
            <a href="#services">Services</a>
            <a href="#contact">Contact</a>
        </nav>
    </header>

    <!-- Main Content Area (CSS Grid) -->
    <div class="container">
        <!-- Sidebar Section -->
        <aside class="sidebar">
            <h2>Sidebar</h2>
            <p>This is the sidebar area with additional links and information.</p>
        </aside>

        <!-- Main Content Section -->
        <main class="content">
            <h2>Main Content</h2>
            <p>Welcome to the main content area. This section holds the primary information.</p>
        </main>
    </div>

    <!-- Footer -->
    <footer>
        <p>&copy; 2023 My Webpage. All rights reserved.</p>
    </footer>

</body>
</html>
